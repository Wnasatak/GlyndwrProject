package assignment1.krzysztofoko.s16001089.data

import android.util.Log

/**
 * Seeder function is currently cleared to prevent pushing any additional data.
 * All data is loaded from the assets database.
 */
suspend fun seedDatabase(db: AppDatabase) {
    Log.d("DatabaseSeeder", "Seeder is clear. No data pushed.")
}
